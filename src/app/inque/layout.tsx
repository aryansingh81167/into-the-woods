
'use client';

// This layout file can be used for any shared UI across the /inque routes.
// For now, we are keeping it simple.

export default function InqueLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
